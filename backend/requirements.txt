# Backend dependencies for DevMind
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Data validation (included with FastAPI, but pinned for clarity)
pydantic>=2.5.0

# Testing
pytest>=7.4.0
httpx>=0.25.0

# Environment variables
python-dotenv>=1.0.0

# Code search (semantic + keyword hybrid)
sentence-transformers
faiss-cpu
rank-bm25
